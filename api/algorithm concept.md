# first layer
```php
function bfs_all(array $graph): array {
    $checked = [];
//    save the route which is been checked
    $layout   = [];
    foreach ($graph as $start => $_) {
//    second part
    }
}
```
# second layer
```php
```